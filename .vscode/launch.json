{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Manajemen RT RW (App)",
            "cwd": "${workspaceFolder}/frontend_flutter", 
            "request": "launch",
            "type": "dart",
            "program": "${workspaceFolder}/frontend_flutter/lib/main.dart"
        }
    ]
}